/**
 * 1. do not who the link to those user who should not see the link
 * 2. event if they get the link, do not allow them to visit the link
 * we will secure it with admin route
 * 3. do not allow user to access the api. check admin in the server as well
 * (verifyAdmin)
 */
